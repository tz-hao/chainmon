/**
 * Demo identity (Phase 2) — real authentication arrives in Phase 7 (Privy).
 * All demo-user logic lives here so fixed ids never leak into components.
 */

export const DEMO_EMAIL = "demo@chainmon.local";

export const DEMO_MODE_LABEL = "Demo Trainer";
