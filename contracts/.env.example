# ChainMon Contracts — environment template (committed, NO real secrets).
# Copy to contracts/.env for real deployments (never commit .env — gitignored).

# --- Monad Testnet (primary public testnet target, chainId 10143) ---
MONAD_TESTNET_RPC_URL=https://testnet-rpc.monad.xyz
MONAD_DEPLOYER_PRIVATE_KEY=
MONADSCAN_API_KEY=

# --- Historical: Base Sepolia (earlier planned target, kept for reference) ---
BASE_SEPOLIA_RPC_URL=
DEPLOYER_PRIVATE_KEY=
